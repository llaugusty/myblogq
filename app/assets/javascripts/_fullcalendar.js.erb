
$(document).ready(function() {

    $('#calendar').fullCalendar({
    	events: [
    		{
    			title: 'event',
    			start: '2015-07-10'
    		},
    		{
    			title: 'event',
    			start: '2015-07-10'
    		}
    	],
	   	eventRender: function(event, elements) {
	      	elements.bind('dblclick', function() {
	      		var e = prompt('Enter the event', $(this).text());

	      		if (e != null) 
	      			$(this).text(e);
	      	});
	   	}
	});
});